apiVersion: v1
kind: Service
metadata:
  name: boardgame-svc
spec:
  type: ClusterIP
  selector:
    app: boardgame
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
