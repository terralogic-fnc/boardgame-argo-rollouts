apiVersion: v1
kind: Service
metadata:
  name: boardgame-svc-preview
spec:
  type: ClusterIP
  selector:
    app: boardgame
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}

